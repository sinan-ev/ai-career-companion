You are a senior data analyst. Given the dataset schema below, decide which
analyses would reveal the most meaningful insights.

Dataset: {dataset_description}
Numeric columns: {numeric_columns}
Categorical columns: {categorical_columns}
Datetime columns: {datetime_columns}

Column meanings:
{column_meanings}

Return ONLY a JSON array of analysis types. Choose from:
["trend", "comparison", "correlation", "distribution", "top_n", "time_series"]

Rules:
- Include "time_series" only if datetime columns exist
- Include "correlation" only if >= 2 numeric columns exist
- Include "comparison" only if >= 1 categorical and >= 1 numeric column exist
- Maximum 4 analysis types
- Order by expected insight value (most valuable first)

Return JSON array only. No explanation.
